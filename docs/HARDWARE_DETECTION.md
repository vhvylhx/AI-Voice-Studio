# Hardware Detection

HardwareDetectionService chi phat hien thong tin an toan:

- OS, CPU name, CPU threads;
- RAM total;
- FFmpeg/FFprobe availability;
- NVIDIA GPU/VRAM qua `nvidia-smi` voi timeout.

Khong doc process list, serial number, token, driver private data hoac du lieu Project/Voice.

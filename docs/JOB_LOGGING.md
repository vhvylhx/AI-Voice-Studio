# Job Logging

Moi Job co log rieng: `workspace/jobs/jobs/<job_id>/job.log`.

Moi dong log la JSON line gom timestamp, level, job_id, stage, message va data.

Khong log secret hoac raw sensitive payload.
